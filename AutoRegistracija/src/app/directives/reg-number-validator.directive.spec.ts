import { RegNumberValidatorDirective } from './reg-number-validator.directive';

describe('RegNumberValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new RegNumberValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
