import { GimimoValidationDirective } from './gimimo-validation.directive';

describe('GimimoValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new GimimoValidationDirective();
    expect(directive).toBeTruthy();
  });
});
